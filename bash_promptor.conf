#
# bash_promptor configuration
#
# Uncomment and modify values to customize your prompt.
# Use `bash_promptor_config_list` to see all available options.
# Use `bash_promptor_reload` to apply changes.
#

# --- Prompt Layout ---
# Available segments: exit_code user host path git_async git
# prompt=exit_code user host path git_async

# Powerline glyph style (use `bash_promptor_glyphs` to see all)
# prompt.glyph=left_hard_divider

# --- Path ---
# Style: full, short, basename
# path.style=short
